# 2026-03-28 — Crate rename: lib/app/daemon naming convention

## What

Renamed all 24 crates to follow a naming convention that makes crate type visible from the name alone:

- `cosmix-lib-*` — libraries (Rust module name preserved as `cosmix_*` via `[lib] name`)
- `cosmix-*` — GUI apps (Dioxus desktop/web)
- `cosmix-*d` — headless daemons/services

## Rename map

| Old | New | Reason |
|-----|-----|--------|
| cosmix-port | cosmix-lib-amp | lib + name matches the AMP protocol |
| cosmix-client | cosmix-lib-client | lib |
| cosmix-config | cosmix-lib-config | lib |
| cosmix-mesh | cosmix-lib-mesh | lib |
| cosmix-ui | cosmix-lib-ui | lib |
| cosmix-jmap | cosmix-maild | daemon + name matches the role, not the protocol |
| cosmix-hub | cosmix-hubd | daemon |
| cosmix-embed | cosmix-indexd | daemon + name matches the intent (indexing), not the mechanism (embeddings) |
| cosmix-web | cosmix-webd | daemon |
| cosmix-launcher | cosmix-menu | app + stripped of Lua script management |
| (new) | cosmix-scripts | Lua + Bash script manager, extracted from cosmix-launcher |

## Key decisions

- **Flat directory structure retained** — naming convention provides visual grouping without subdirectories. Can move to `apps/`, `libs/`, `daemons/` folders later if the workspace grows past ~100 crates.
- **Rust module names preserved** for all libs except cosmix-port → cosmix_amp. This means zero source changes for the 4 libs that kept their module name. Only 5 .rs files needed `use cosmix_port` → `use cosmix_amp`.
- **Cargo.toml `package` key** used for lib deps: `cosmix_ui = { package = "cosmix-lib-ui", path = "..." }` — keeps `use cosmix_ui::` working while the crate name on disk is `cosmix-lib-ui`.
- **cosmix-launcher split** into cosmix-menu (pure tray app launcher) + cosmix-scripts (CLI script manager with bash support). Scripts dir moved from `~/.local/lua/` to `~/.local/scripts/`.
- **`_doc/` and `_journal/` left as historical record** — they document what was true when written.

## Scope

- 10 directory renames + 1 new crate
- 24 Cargo.toml files updated (root + renamed + consumers)
- 5 Rust source files updated (cosmix_port → cosmix_amp)
- 6 shell scripts updated (build-*, deploy, setup, migrate)
- CLAUDE.md fully updated with new workspace table

---

# 2026-03-28 (session 2) — Refactoring complete, live theme reactivity, hub plumbing fixed

## Refactoring Phases 1-6 (complete)

- **Phase 1-2**: Extracted `app_init` module (THEME signal, use_theme_css, use_theme_poll, launch_desktop) and shared `components` (StatusBar, ErrorBanner, TableHeader) into cosmix-lib-ui
- **Phase 3**: Migrated all 13 GUI apps to shared theme infrastructure (-323 LOC). Tier A (7 apps, dedup) and Tier B (4 apps, hardcoded hex to OKLCH CSS vars + MenuBar)
- **Phase 4**: Added `use_hub_client`/`use_hub_handler` hooks, migrated 8 apps (-96 LOC). Auto config.watch registration and config.changed handling
- **Phase 5**: Created `cosmix-lib-daemon` crate with `init_tracing()`, `shutdown_signal()`, `load_tls_config()`. Migrated 6 daemons (-55 LOC)
- **Phase 6**: Deleted 18 legacy theme constants, cleaned 20 redundant deps from 8 apps

## Live theme reactivity

- Dioxus `document::Style` is write-once (injects into head on first render, ignores all subsequent prop changes)
- `use_theme_css()` now uses `document::eval()` to create/update a `<style id="cosmix-theme">` element via JavaScript. CSS custom properties update instantly
- Settings writes THEME signal immediately for hue slider, dark toggle, and font_size
- MenuBar and all settings UI use CSS var font sizes instead of hardcoded px

## Hub plumbing fixed

- configd registers as `"config"` but all apps were calling `"configd"` — fixed to `"config"`
- Settings connects to hub, calls `config.reload` on save, configd broadcasts `config.changed` to all watchers
- Rebuilt fresh hubd + configd + mond from `~/.local/bin/`

## cosmix-menu

- LMB click opens menu (MENU_ON_ACTIVATE = true), visible in COSMIC launcher, self-excluded from own menu

## Workspace state

- 25 crates (added cosmix-lib-daemon), all GUI apps have dynamic OKLCH theme + MenuBar + mimalloc + hub connection
- Net LOC reduction: ~500 lines removed across ~45 files

## Next steps

- Comprehensive DataTable component for cosmix-lib-ui (desktop + WASM)
- Redesign cosmix-mon with compact node list into detail view using DataTable
- Hub observability: hub.stats command, AMP traffic inspector
